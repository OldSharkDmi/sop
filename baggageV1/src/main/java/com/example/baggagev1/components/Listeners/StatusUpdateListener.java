package com.example.baggagev1.components.Listeners;
/*
import org.springframework.amqp.rabbit.annotation.RabbitListener;
import org.springframework.stereotype.Service;

@Service
public class StatusUpdateListener {

    @RabbitListener(queues = "baggage.status.change")
    public void receiveStatusUpdate(String message) {
        System.out.println("Received baggage status update: " + message);
    }
}

 */
